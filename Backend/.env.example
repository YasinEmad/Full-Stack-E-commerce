# Backend Environment Variables
# Copy this file to .env and fill in your values

# ============================================
# REQUIRED VARIABLES (Must be set)
# ============================================

# MongoDB Atlas Connection String
# Format: mongodb+srv://username:password@cluster.mongodb.net/database_name?retryWrites=true&w=majority
MONGO_URI=

# Node Environment
NODE_ENV=development

# JWT Secret (use a strong random string)
JWT_SECRET=

# Frontend URL (used for CORS)
# Local: http://localhost:5173
# Production: https://your-frontend-domain.com
FRONTEND_URL=http://localhost:5173

# ============================================
# OPTIONAL VARIABLES (Nice to have)
# ============================================

# Admin Credentials (for dashboard)
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=

# Email Service (Brevo/Sendinblue API)
BREVO_API_KEY=

# Server Port (default: 5000)
PORT=5000

# ============================================
# TIPS FOR PRODUCTION (Vercel)
# ============================================
#
# 1. MONGO_URI: Ensure IP whitelist includes 0.0.0.0/0
#    URL-encode special characters in password: @ → %40, : → %3A
#
# 2. JWT_SECRET: Generate using: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
#
# 3. FRONTEND_URL: Add both dev and prod URLs separated by commas
#    Example: http://localhost:5173,https://your-app.com
#
# 4. Never commit .env to git! Add to .gitignore
#
# 5. In Vercel Dashboard:
#    - Go to Settings → Environment Variables
#    - Add all REQUIRED variables
#    - Select which environments (Preview, Production)
