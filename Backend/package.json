{
  "name": "learn-express",
  "version": "1.0.0",
  "main": "server.js",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "prestart": "node -v | grep -q $(cat .nvmrc) || (echo 'Please use Node.js version' $(cat .nvmrc) && exit 1)",
    "start": "node server.js",
    "predev": "node -v | grep -q $(cat .nvmrc) || (echo 'Please use Node.js version' $(cat .nvmrc) && exit 1)",
    "dev": "nodemon server.js",
    "seed:orders": "node seedOrders.js"
  },
  "dependencies": {
    "axios": "^1.12.2",
    "cookie-parser": "^1.4.7",
    "cors": "^2.8.5",
    "dotenv": "^17.2.0",
    "express": "4.18.2",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.16.3",
    "nodemailer": "^7.0.10",
    "shortid": "^2.2.17"
  },
  "devDependencies": {
    "nodemon": "^3.1.10"
  }
}
